ARCH_FLAG = -m32 
ifeq ($(R_ARCH),/x64) 
  ARCH_FLAG = -m64 
endif 
PKG_CXXFLAGS = $(ARCH_FLAG) -fopenmp
PKG_CFLAGS=$(SHLIB_OPENMP_CFLAGS) 
PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS) $(ARCH_FLAG) $(SHLIB_OPENMP_CFLAGS)
